# Web3Forms API key — get yours at https://web3forms.com
# REQUIRED for Vercel production deploys (build fails without it).
# Also set on Cloudflare Workers Builds so contact/audit forms work in prod.
# Optional for local / GitHub Actions verification builds (form stays disabled).
VITE_WEB3FORMS_KEY=your_key_here
